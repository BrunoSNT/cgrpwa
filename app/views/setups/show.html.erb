<div class="card">
  <div class="card-body">
    <table class="table">
      <thead>
        <tr>
          <th>Horas Cobradas</th>
          <th>Preço do Mandou-superbem</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= @setup.weekly_hours %></td>
          <td><%= @setup.superdidgood_price %></td>
        </tr>
      </tbody>
    </table>
  </div>
</div>

<%= link_to 'Editar', edit_setup_path(@setup), class: "btn btn-warning" %> |
<%= link_to 'Voltar', setups_path, class: "btn btn-primary" %>
