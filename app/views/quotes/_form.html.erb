<%= form_with(model: quote, local: true, html: { class: "form-material", id: ""}) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :imagem %><br>
    <%= form.file_field :image %>
  </div>

  <br>
  <br>

  <div class="form-group">
    <%= form.label :name, "Nome do Orçamento" %>
    <%= form.text_field :name, class: "form-control form-control-line" %>
  </div>

  <div class="form-group">
    <%= form.label :description, "Descrição" %>
    <%= form.text_area :description, class:"form-control", id:"exampleFormControlTextarea1", rows:"3" %>
  </div>

  <div class="form-group">
    <%= form.label :value, "Valor" %>
    <%= form.number_field :value, class: "form-control form-control-line" %>
  </div>

  <div class="actions">
    <%= form.submit "Enviar", class: "btn btn-success" %>
  </div>
<% end %>
